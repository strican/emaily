{
  "name": "emaily",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client-start": "yarn workspace client start",
    "server-start": "yarn workspace server build && yarn workspace server start",
    "dev": "yarn client-start & yarn workspace server dev"
  }
}
